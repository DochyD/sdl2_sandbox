#include <iostream>
#include <SDL2/SDL.h>

void drawRectangle(int x1, int x2, int y1, int y2)
{
	// SDL_Rect fillRect = {x1, x2, y1, y2};
	// SDL_SetRenderDrawColor(gRenderer, 150, 150, 150, 255);
	// SDL_RenderFillRect(gRenderer, &fillRect);
}